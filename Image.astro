---
export interface Props {
	src: string;
	caption?: string;
}

const { src, caption } = Astro.props as Props;
---

<figure class="figure">
	<img src={src} />
	{caption && <caption>{caption}</caption>}
</figure>